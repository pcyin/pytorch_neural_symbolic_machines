name: pytorch_nsm
channels:
  - pytorch
  - defaults
dependencies:
  - python=3.6
  - pytorch=1.3
  - torchvision
  - numpy
  - nltk==3.3
  - jupyter
  - docopt
  - pandas
  - pip
  - pip:
    - tqdm
    - bloom-filter==1.3
    - Babel==2.5.3
    - gensim==3.2.0
    - tensorboardX
    - editdistance

# Also install the TaBERT library
